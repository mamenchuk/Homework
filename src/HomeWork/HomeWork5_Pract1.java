package HomeWork;

public class HomeWork5_Pract1 {
    public static void main (String[] args) {
        int[] arr = {-1, 2, -3, 4, 5, 6, -7, -8, 9, 0};

        // число з найбільшим значенням,    ВЕЗДЕ     рассказать мне что значит каждая строчка!!!!!!!
        int max = arr[0];
        if (arr[i] > max) {
            max = arr[i];
        }
        System.out.println("maximum = " + max);


// summ of positive numbers,
        int b = 0;
        for (int i = 0; b < 10; b++) {
            if (arr[b] > 0) {
                b = b * arr[b];
            }
        }
        System.out.println("sum of positive = " + b);


        // ammount_of negative numbers
        int i = 0;
        for (arr[i] < 0) {
            neg_ammoun = i - arr[i];
            system.out.print.in("amount of negative numbers = " + neg_ammount)
        }



    

    }